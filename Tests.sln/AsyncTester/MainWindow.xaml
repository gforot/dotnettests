<Window x:Class="AsyncTester.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AsyncTester"
        mc:Ignorable="d"
        Title="MainWindow" Height="242.308" Width="670.192">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Click="Button_Click" Margin="10">Click me!</Button>
        <RichTextBox x:Name="myRichTextBox" Grid.Column="1" Margin="10" Grid.RowSpan="2"/>

        <ProgressBar x:Name="myProgressBar" Grid.Row="1" Margin="10" />
    </Grid>
</Window>
